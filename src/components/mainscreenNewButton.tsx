import { Button } from "./ui/button";

const MainScreenNewButton = () => {
  return (
    <div className="bg-gray-400 flex justify-end">
      <Button className="h-6 bg-transparent">Add New</Button>
    </div>
  );
};

export default MainScreenNewButton;
