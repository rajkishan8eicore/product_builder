import { Button } from "./ui/button";

const MainScreenNewButton = () => {
  return (
    <div className="bg-gray-400 dark:bg-gray-800 flex justify-end">
      <Button className="h-6 bg-transparent text-white">Add New</Button>
    </div>
  );
};

export default MainScreenNewButton;
